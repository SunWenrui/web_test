# 📝 数据持久化说明

## ✅ 好消息：数据已经可以永久保存了！

你的网页**已经具备记忆操作的功能**，所有数据都会自动保存到数据库。

## 🔍 如何验证数据持久化？

### 方法一：简单测试

1. **启动服务器**
   ```bash
   npm start
   ```

2. **在网页上添加数据**
   - 添加一些行为、勋章、用户

3. **关闭浏览器**
   - 完全关闭浏览器窗口

4. **重新打开浏览器**
   - 访问 `http://localhost:3000`
   - **数据应该还在！** ✅

5. **重启服务器**
   ```bash
   # 按 Ctrl+C 停止服务器
   # 然后重新启动
   npm start
   ```
   - **数据应该还在！** ✅

### 方法二：使用测试脚本

```bash
npm test
```

这会显示数据库状态和数据统计。

## 💾 数据存储位置

所有数据保存在：
```
项目目录/medal_system.db
```

这是一个 SQLite 数据库文件，包含所有操作记录。

## 🌐 让其他人也能访问（部署到公网）

### 最快的方法：使用 ngrok（5分钟）

1. **安装 ngrok**
   ```bash
   brew install ngrok  # macOS
   # 或访问 https://ngrok.com/download
   ```

2. **启动你的服务器**
   ```bash
   npm start
   ```

3. **在另一个终端运行**
   ```bash
   ngrok http 3000
   ```

4. **获得公网地址**
   - ngrok 会显示一个网址，例如：`https://abc123.ngrok.io`
   - 把这个网址分享给朋友即可

### 长期使用：部署到 Railway（推荐）

1. 访问 https://railway.app
2. 用 GitHub 登录
3. 创建新项目，连接你的代码仓库
4. Railway 会自动部署，给你一个固定网址

详细步骤见 [DEPLOY.md](./DEPLOY.md)

## 🔄 数据备份

### 自动备份

```bash
npm run backup
```

这会：
- 创建备份文件到 `backups/` 目录
- 自动压缩备份
- 清理7天前的旧备份

### 手动备份

直接复制数据库文件：
```bash
cp medal_system.db medal_system_backup_$(date +%Y%m%d).db
```

## ❓ 常见问题

**Q: 数据真的不会丢失吗？**
A: 是的！所有操作都会立即保存到数据库文件。只要数据库文件不删除，数据就不会丢失。

**Q: 如何恢复数据？**
A: 如果有备份文件，直接替换 `medal_system.db` 即可。

**Q: 数据库文件在哪里？**
A: 在项目根目录，文件名是 `medal_system.db`

**Q: 可以查看数据库内容吗？**
A: 可以，使用 SQLite 工具：
```bash
sqlite3 medal_system.db
.tables
SELECT * FROM behaviors;
```

## 📋 总结

✅ **数据持久化已启用** - 所有操作自动保存  
✅ **关闭浏览器不丢失** - 数据在服务器上  
✅ **重启服务器不丢失** - 数据在数据库文件中  
✅ **可以部署到公网** - 让其他人访问  
✅ **支持数据备份** - 使用 `npm run backup`

## 🚀 下一步

1. **测试数据持久化**：运行 `npm test`
2. **部署到公网**：查看 [DEPLOY.md](./DEPLOY.md)
3. **设置自动备份**：使用 cron 定时运行备份脚本

---

**记住**：只要 `medal_system.db` 文件存在，你的所有操作都会被记住！🎉

