# 快速启动指南

## 第一步：安装依赖

```bash
npm install
```

## 第二步：启动服务器

```bash
npm start
```

看到以下信息表示启动成功：
```
数据库连接成功
数据库表初始化完成
服务器运行在 http://localhost:3000
```

## 第三步：打开浏览器

访问：`http://localhost:3000`

## 功能说明

### 实时同步
- 打开多个浏览器窗口或标签页
- 在一个窗口中添加/修改/删除数据
- 其他窗口会自动实时更新，无需刷新

### 数据持久化
- 所有数据保存在 `medal_system.db` 文件中
- 关闭服务器后数据不会丢失
- 重新启动服务器后数据仍然存在

### 多用户协作
- 多个用户可以同时访问网站
- 每个人的操作都会实时同步给其他用户
- 支持同时编辑，不会冲突

## 常见问题

**Q: 端口被占用怎么办？**
A: 修改 `server.js` 最后一行，或设置环境变量：
```bash
PORT=3001 npm start
```

**Q: 如何备份数据？**
A: 复制 `medal_system.db` 文件即可

**Q: 如何重置所有数据？**
A: 删除 `medal_system.db` 文件，重启服务器会自动创建新数据库

